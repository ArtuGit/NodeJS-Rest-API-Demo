name: Node.js Build, Test, and Deploy

on:
  push:
    branches: [ staging ]
  pull_request:
    branches: [ staging ]

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [14.x]
    steps:
      - shell: bash
        env:
          ENV_SECRET: ${{ secrets.ENV }}
        run: |
          echo "$ENV_SECRET"
          echo "$ENV_SECRET" >> .env
          ls -la
          cat .env

